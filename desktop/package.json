{
  "name": "tmall-dashboard-desktop",
  "version": "1.0.12",
  "private": true,
  "description": "天猫数据仪表盘桌面版",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "dist": "npm run build && electron-builder --config electron-builder.yml --win nsis --x64 --publish never",
    "dist:mac": "npm run build && electron-builder --config electron-builder.yml --mac dmg zip --publish never"
  },
  "dependencies": {
    "electron-updater": "6.3.9"
  },
  "devDependencies": {
    "@types/node": "24.12.2",
    "electron": "42.3.0",
    "electron-builder": "26.15.3",
    "typescript": "5.6.3",
    "vitest": "3.2.4"
  }
}
