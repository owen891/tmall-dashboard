# 更新说明

## V1.0.13

本版本聚焦运营动作闭环、真实经营数值展示、商品批量操作和桌面发布一致性。

### 新增与改进

- 优化运营动作内容展示，分层呈现动作目的、执行内容、计划日期、状态、负责人和观察窗口。
- 新增正式运营动作删除接口，支持版本校验、状态限制、能力门禁和审计快照；旧数字动作接口继续保持只读兼容。
- 修复计划日历无数据时清空月份网格的问题，仍可切换月份、选择日期并查看明确的空状态。
- 趋势图改为展示净销售额、支付金额和推广花费的实际值，不再使用首日归一化指数。
- 商品列表新增批量复制商品 ID，按选中顺序逐行复制，并提供剪贴板降级方案与操作反馈。
- 补齐动态操作的页面能力门禁，区分 `products.list` 只读能力与商品目录编辑能力。
- 同步 Web、Windows 和 macOS 桌面版本号，当前版本统一为 `1.0.13`。
- 强化本地数据保护规则，数据库、备份、导入文件、日志、截图和运行中间产物不会进入发布提交。

### 验证

- Python 完整测试集通过。
- 前端契约、页面能力和 UI demo 校验通过。
- JavaScript 语法检查与 `git diff --check` 通过。
- 商品列表批量复制已在本地浏览器中实际验证。
- 桌面发布 workflow 的 Windows/macOS 构建和更新元数据流程已校验；本次源码同步不在本机生成桌面安装包。

### 升级提示

#### Web 版

- 页面启动、回到前台和定时任务会请求 `/api/version?client=web`。
- 服务端版本高于页面内置版本时，顶部显示更新说明链接和“立即刷新”；刷新只重新加载服务器已部署的资源。
- 浏览器不会覆盖服务器文件，管理员仍需通过服务器部署流程完成 Web 版升级。

#### Windows/macOS 桌面版

- Electron 启动时自动检查 GitHub Releases；托盘菜单和设置页支持手动检查。
- 发现更新后先确认下载，下载完成后再确认重启安装；安装前停止内置 Flask/Waitress 服务，避免运行中的后端文件被占用。
- Windows 更新索引为 `latest.yml`，并与 `.exe`、`.exe.blockmap` 一起发布；macOS 更新索引为合并后的 `latest-mac.yml`，并与 x64/arm64 的 `.dmg`、`.zip`、`.blockmap` 一起发布。
- `desktop-release.yml` 使用 Windows 与 macOS 原生 runner 构建对应产物，再由单一发布 job 上传到同一个 GitHub Release。当前 `v1.0.13` Release 已包含两平台更新索引和安装包。
- 发布 tag 必须与根目录版本严格一致：`VERSION=1.0.13` 对应 `v1.0.13`。开发环境不会执行桌面在线更新检查。
- macOS 自动安装还依赖有效的 Apple Developer 签名和公证配置；未配置时仍可手动下载，但系统可能阻止未签名应用或自动安装。

## V1.0.2

本版本聚焦运营动作闭环、统一提醒体验、本地数据扫描安全和发布一致性。

### 新增与改进

- 新增正式的“新增运营动作”入口，统一使用 `POST /api/actions` 创建动作，支持商品、动作类型、执行计划、目标指标和观察窗口等字段。
- 将运营动作待办、运营动作日志、异常提醒和计划日历整合为统一的运营提醒中心，支持按提醒类型筛选和手动刷新。
- 提醒列表与计划日历复用同一条正式运营动作数据，避免同一动作重复展示；日历查询失败时展示明确错误和重试入口。
- 完善页面能力门控，运营动作创建不再依赖无关的日度数据；前端、后端和页面能力目录统一使用 `overview.create_action`。
- 完善本地目录扫描：增加 Web/桌面端边界说明、受控目录 allowlist、路径穿越与符号链接校验、查看/管理权限和稳定错误码。
- 优化字段设置弹窗的宽度、预览区域、排序操作、长文本换行和移动端布局。
- 改进导入、数据能力、页面能力和动作接口的统一响应格式、错误处理与审计信息。
- 更新桌面端与 Web 端版本同步机制，当前版本统一为 `1.0.2`。

### 验证

- Python 单元与集成测试：625 项通过。
- 前端 JavaScript 语法检查、UI 静态契约和核心页面浏览器 smoke 通过。
- 概览页正式运营动作创建、桌面端/移动端布局、焦点恢复和横向溢出检查通过。
- 发布审计确认数据库完整性正常，正式数据来源血缘完整，无已跟踪敏感产物。

### 升级提示

Web 版由服务器部署流程升级；桌面版从 GitHub Releases 获取安装包和在线更新。发布标签应与根目录 `VERSION` 文件保持一致：`VERSION=1.0.2` 对应 `v1.0.2`。
