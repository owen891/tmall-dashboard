# 更新说明

## V2.0.0（本地开发版，尚未发布安装包）

本版本是从旧版仪表盘向“经营分析 + 商品运营 + 数据治理”工作台的整体升级。当前代码基线为 `ddd5339`（2026-09-15）；GitHub 目前还没有对应的 `v2.0.0` Release 或桌面安装包。

### 产品与页面体系

- 将总览、商品运营、推广分析、生命周期、经营复盘、数据中心和设置整理为统一的正式页面体系，移除旧版模板、原型脚本和重复的静态页面入口。
- 总览升级为经营决策台：增加指标卡、趋势对比、目标进度、重点商品、数据证据和运营提醒等结构化区域。
- 商品详情补齐日度趋势、周期对比、贡献分析、生命周期历史和运营动作闭环，支持从分析直接进入复盘。
- 推广页面按渠道、计划、单元、创意、地域和商品等已导入粒度提供下钻；趋势图直接展示净销售额、支付金额和推广花费实际值。
- 统一弹窗、表格、分页、字段设置、移动端布局、焦点恢复和横向滚动交互，并将图标资源本地化。

### 数据能力与接口契约

- API 统一使用成功、失败、证据、可用性和限制信息的响应封装，明确区分 `available`、`partial` 和 `no-data`。
- 增加月度、周度、日度周期参数校验，补齐日期范围、商品、店铺和分页过滤的一致性，非法请求返回稳定的验证错误。
- 客户分析、KPI、漏斗、行业基准、关键词和推广分析改用真实事实数据；缺失字段不再以 `0` 或推测值填充。
- 增加商品批量操作、批量复制商品 ID、标签、收藏、目标、日历、复盘、提醒和运营动作等正式接口，并保留写入审计、版本冲突保护和状态限制。
- 统一页面能力目录与请求级能力门控，区分只读列表能力和商品目录编辑能力；只有具备真实数据前提的功能才会开放。

### 品类模式与经营数据

- 新增品类模式，将 BI 商品标签接入导入、目标进度、动作、商品列表和推广链路，支持按品类查看经营数据。
- 新增派米 BI 日度、概览和月度导入脚本，支持标准商品参考资料同步、商品标签导入和商品图片修复/校验。
- 目标体系支持净销售额目标，并在月度数据下提供日度回退计算；总览趋势补齐 `payment_amount` 别名，确保支付金额曲线正常渲染。
- 运营动作列表补充目的、执行内容、计划日期、状态、负责人和观察窗口；新增正式删除接口并保留审计快照。

### 运营闭环

- 新增正式的“新增运营动作”入口，记录商品、动作类型、计划日期、动作详情、目的、观察窗口和目标指标。
- 将待办动作、异常提醒、系统通知和计划日历合并为运营提醒中心，支持筛选、刷新、日历定位和观察窗口复盘。
- 修复计划日历无数据时月份网格被清空的问题，仍可切换月份、选择日期并查看明确的空状态。
- 运营动作统一通过 `POST /api/actions` 创建，并与商品详情、经营复盘和提醒列表复用同一份正式数据。

### 数据接入与治理

- 重构 Excel、旧版 HTML `.xls`、CSV 和 ZIP 导入链路，增加异步进度、预览、字段映射、质量门禁、批次历史和安全撤销。
- 新增受控目录扫描：allowlist、路径穿越校验、符号链接校验、稳定文件判定、重试、查看/管理权限和稳定错误码。
- 引入来源 observation/lineage、批次指纹和冲突拒绝机制，支持对历史数据进行可核验回填而不覆盖冲突记录。
- 增加店铺范围隔离；不支持的店铺范围显式返回 `UNSUPPORTED_SCOPE`，避免静默串店。

### 安全、运行与发布

- 增加统一异常处理、脱敏日志、数据库完整性检查、生产预检、备份恢复和发布审计门禁。
- Web 版和桌面版共享同一套版本检查与更新提示；桌面端保留 Windows/macOS 构建、运行时后端和在线更新能力。
- 发布流水线增加版本同步、标签校验、依赖锁定、浏览器 smoke、核心交互审计和敏感产物检查。
- 清理旧版 `templates/`、`static/` 仪表盘实现及历史原型文档，避免新旧两套页面和数据契约并存。

### 验证范围

- 保留并扩展 Python API、导入、品类模式、店铺隔离、桌面运行时、发布契约和前端页面测试。
- 增加核心页面浏览器 smoke、响应式视口、图表输出、错误状态、横向溢出和交互审计检查。
- 正式发布前仍需在干净工作树执行完整测试、生产预检、数据库审计和桌面构建；本地开发版尚未生成 `v2.0.0` 安装包。

### 升级提示

- 当前本地开发基线：`VERSION=2.0.0`，提交 `ddd5339`。
- 当前 GitHub 最新正式安装包仍为 `v1.0.13`；不要将其与本地 `2.0.0` 代码混用。
- 发布 `v2.0.0` 前必须先完成完整验证，再创建与 `VERSION` 一致的 `v2.0.0` 标签并由 GitHub Actions 构建安装包。

## V1.0.13（已发布）

本版本聚焦运营动作闭环、真实经营数值展示、商品批量操作和桌面发布一致性。GitHub Release 已提供 Windows 与 macOS 安装包。

## V1.0.2（历史版本）

本版本聚焦运营动作闭环、统一提醒体验、本地数据扫描安全和发布一致性。
