# 天猫数据仪表盘前端

本目录是 Flask 正式页面使用的原生 HTML/CSS/JavaScript 前端资源。页面通过正式 API 读取当前数据，缺失数据显示为空态，不内置 mock 或演示记录。

启动 Flask 应用后从 `/`、`/products`、`/promotion`、`/lifecycle`、`/reviews`、`/data-center`、`/settings` 访问正式页面。
